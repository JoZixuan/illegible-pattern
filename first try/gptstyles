/* styles.css */
body {
    margin: 0;
    font-family: Arial, sans-serif;
    background-color: white; /* Default: Light Mode */
    color: black;
    transition: background-color 0.3s, color 0.3s;
}

.dark-mode {
    background-color: black;
    color: white;
}

#toggle-mode {
    position: fixed;
    top: 10px;
    right: 10px;
    padding: 10px 20px;
    border: none;
    background-color: gray;
    color: white;
    cursor: pointer;
    border-radius: 5px;
    z-index: 1000; /* Ensures it's always visible */
}

#toggle-mode:hover {
    background-color: darkgray;
}

/* Multi-column shape container */
#shape-container {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(100px, 1fr)); /* Dynamic multi-column layout */
    gap: 10px;
    padding: 10px;
}

/* General shape styling */
.shape {
    display: flex;
    align-items: center;
    justify-content: center;
}
